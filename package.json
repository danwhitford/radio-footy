{
  "name": "radio-footy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node getFeed.ts && ts-node buildSite.ts",
    "run:watch": "ls template.pug site/filter.js | entr npm run build:site",
    "prepublishOnly": "cp -f site/* docs/",
    "publish": "git commit -am 'publish' && git push",
    "format": "prettier -w .",
    "build:site": "ts-node buildSite.ts",
    "get:feed": "ts-node getFeed.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.10",
    "cross-fetch": "^3.0.6",
    "date-fns": "^2.16.1",
    "date-fns-tz": "^1.0.12",
    "prettier": "^2.1.2",
    "pug": "^3.0.0",
    "rxjs": "^6.6.3",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
