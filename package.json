{
  "name": "radio-footy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "get": "ts-node get_data.ts",
    "build": "ts-node build_site.ts",
    "build:watch": "ls template.pug | entr npm run build",
    "prepublishOnly": "cp -f site/* docs/",
    "publish": "git commit -am 'publish' && git push",
    "format": "prettier -w ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.10",
    "cross-fetch": "^3.0.6",
    "date-fns": "^2.16.1",
    "date-fns-tz": "^1.0.12",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.1.2",
    "pug": "^3.0.0",
    "rxjs": "^6.6.3",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
